import React from "react";
// a function, Comment, is declared
// make a component to display a single user's comment
function Comment() {
  // Here is the HTML that this would create when added to the DOM
  return <div>Naturally, I agree with this article.</div>;
}

export default Comment;
